const db = require('./../core/db')
const tableName = 'leave'
const util = require('./../utils/util')

const leaveCount = async (query) => {
    return new Promise((reslove, reject) => {
        console.log('sdgksjgksdjjgsd')
        console.log(query)
    })
}

module.exports = {
    leaveCount
}